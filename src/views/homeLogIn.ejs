<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Pagina de inicio</title>
    <%include partials/head.ejs %>

</head>

<body>

    <header>
        <div class="encabezado">

            <div class="logo">
                <img class="centrado animated infinite pulse" src="http://localhost:3000/images/recreap-02.jpg"
                    alt="Logo recreap" height=”230” width=170”;>
            </div>

            <div class="tituloExtendido">
                <span>Aplicación de tecnología digital en el análisis didáctico de clases en la práctica pre profesional
                    de la
                    formación de docentes.</span>
            </div>

        </div>

        <% if(isAuthenticated && user.rol=== "Administrador"){ %>
        <%include partials/navLogInAdmin.ejs%>
        <% }else {%>
        <% include partials/navLogIn.ejs %>
        <% }%>
    </header>


    <!-- ================== 1-carousel bootstrap  ==================  -->

    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-sm-8">

                <div id="demo" class="carousel slide" data-ride="carousel">

                    <!-- Indicators -->
                    <ul class="carousel-indicators">
                        <li data-target="#demo" data-slide-to="0" class="active"></li>
                        <li data-target="#demo" data-slide-to="1"></li>
                        <li data-target="#demo" data-slide-to="2"></li>
                    </ul>

                    <!-- The slideshow -->
                    <div class="carousel-inner">
                        <div class="carousel-item active">
                            <img class="d-block" src="/images/imagenesSlider/img (1).jpeg" alt="First slide">

                        </div>
                        <div class="carousel-item">
                            <img class="d-block" src="/images/imagenesSlider/IMG-20190610-WA0003.jpg"
                                alt="Second slide">
                        </div>
                        <div class="carousel-item">
                            <img class="d-block" src="/images/imagenesSlider/IMG-20190610-WA0006.jpg" alt="Third slide">
                        </div>
                    </div>

                    <!-- Left and right controls -->
                    <a class="carousel-control-prev" href="#demo" data-slide="prev">
                        <span class="carousel-control-prev-icon"></span>
                    </a>
                    <a class="carousel-control-next" href="#demo" data-slide="next">
                        <span class="carousel-control-next-icon"></span>
                    </a>

                </div>
            </div>
        </div>
    </div>

    <!-- SECCIÓN NOTICIAS -->
    <% if (data) { %>
    <% for(var i = 0; i < data.length; i++) { %>
    <div class="noticia-div">
        <img class="img-noticia" src="<%= data[i].imagen %>" class="img-fluid" alt="...">

        <h2><%= data[i].titulo %></h1>
            <p><%= data[i].contenido %> </p>

            <p>Publicado el:
                <%= data[i].fechaPub.getDate() + "/" + (data[i].fechaPub.getMonth() +1) + "/" + data[i].fechaPub.getFullYear() %>
            </p>

    </div>
    <% } %>
    <% } %>




    <footer>
        <div class="footer">
            <p class="text-right"><i class="fas fa-envelope"></i> recyreap@gmail.com</p>

            <p class="text-center"><i class="fas fa-map-marker-alt"></i> Batlle y Ordoñez 810</p>

            <!-- Add font awesome icons -->
            <a href="#" class="fa fa-facebook"></a>
            <a href="#" class="fa fa-twitter"></a>
            <a href="#" class="fa fa-instagram"></a>
        </div>


        <%include partials/footer.ejs %>
    </footer>
</body>

</html>